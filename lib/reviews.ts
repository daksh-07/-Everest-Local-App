import { supabase, requireSupabaseConfig } from './supabase';
export async function createReview(input:{businessId:string;productId?:string;bookingId?:string;orderId?:string;rating:number;body?:string;photoUrls?:string[]}){requireSupabaseConfig();const {data,error}=await supabase.rpc('create_transaction_review',{p_business_id:input.businessId,p_product_id:input.productId??null,p_booking_id:input.bookingId??null,p_order_id:input.orderId??null,p_rating:input.rating,p_body:input.body??null,p_photo_urls:input.photoUrls??[]});if(error)throw new Error(error.message);return data as string;}
