import { useEffect,useState } from 'react';
import { ActivityIndicator,Pressable,ScrollView,StyleSheet,Text,View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { router } from 'expo-router';
import { myBusiness } from '@/lib/catalog';
import { supabase } from '@/lib/supabase';

type Business={id:string;name:string;status:string;verification_status:string;suburb:string|null;city:string|null;state:string|null};type Counts={opportunities:number;quotes:number;bookings:number;products:number;orders:number;reviews:number};
export default function BusinessDashboard(){const [business,setBusiness]=useState<Business|null>(null);const [counts,setCounts]=useState<Counts>({opportunities:0,quotes:0,bookings:0,products:0,orders:0,reviews:0});const [loading,setLoading]=useState(true);const [error,setError]=useState('');useEffect(()=>{let active=true;(async()=>{try{const b=await myBusiness();if(!b){if(active)setLoading(false);return;}const businessData=b as Business;const queries=[['opportunities',supabase.from('opportunities').select('id',{count:'exact',head:true}).eq('business_id',businessData.id)],['quotes',supabase.from('quotes').select('id',{count:'exact',head:true}).eq('business_id',businessData.id)],['bookings',supabase.from('bookings').select('id',{count:'exact',head:true}).eq('business_id',businessData.id)],['products',supabase.from('products').select('id',{count:'exact',head:true}).eq('business_id',businessData.id)],['orders',supabase.from('orders').select('id',{count:'exact',head:true}).eq('business_id',businessData.id)],['reviews',supabase.from('reviews').select('id',{count:'exact',head:true}).eq('business_id',businessData.id)]] as const;const results=await Promise.all(queries.map(([,q])=>q));const failed=results.find(r=>r.error);if(failed?.error)throw new Error(failed.error.message);if(active){setBusiness(businessData);setCounts(Object.fromEntries(queries.map(([key],i)=>[key,results[i].count??0])) as Counts)}}catch(e){if(active)setError(e instanceof Error?e.message:'Unable to load business dashboard.')}finally{if(active)setLoading(false)}})();return()=>{active=false}},[]);if(loading)return <SafeAreaView style={s.safe}><ActivityIndicator style={{marginTop:60}}/></SafeAreaView>;if(!business)return <SafeAreaView style={s.safe}><View style={s.center}><Text style={s.title}>Business account</Text><Text style={s.copy}>{error||'Create your business profile to access marketplace tools.'}</Text><Pressable style={s.button} onPress={()=>router.push('/business')}><Text style={s.buttonText}>CREATE BUSINESS</Text></Pressable></View></SafeAreaView>;return <SafeAreaView style={s.safe}><ScrollView contentContainerStyle={s.page}><Text style={s.eyebrow}>BUSINESS • EVEREST LOCAL</Text><Text style={s.title}>{business.name}</Text><View style={s.status}><Text style={s.statusLabel}>VERIFICATION</Text><Text style={s.statusValue}>{business.verification_status}</Text><Text style={s.copy}>{[business.suburb,business.city,business.state].filter(Boolean).join(', ')}</Text></View><View style={s.grid}>{Object.entries(counts).map(([key,value])=><View style={s.card} key={key}><Text style={s.number}>{value}</Text><Text style={s.cardLabel}>{key.toUpperCase()}</Text></View>)}</View><Text style={s.section}>Manage marketplace</Text><Action label="Opportunities & quotes" onPress={()=>router.push('/opportunities')}/><Action label="Products & inventory" onPress={()=>router.push('/products')}/><Action label="Customer orders" onPress={()=>router.push('/business-orders')}/><Action label="Business verification" onPress={()=>router.push('/business-verification')}/>{!!error&&<Text style={s.error}>{error}</Text>}</ScrollView></SafeAreaView>}
function Action({label,onPress}:{label:string;onPress:()=>void}){return <Pressable style={s.action} onPress={onPress}><Text style={s.actionText}>{label}</Text><Text>›</Text></Pressable>}
const s=StyleSheet.create({safe:{flex:1,backgroundColor:'#f8f7f4'},page:{padding:20,paddingBottom:50},center:{flex:1,padding:28,justifyContent:'center',alignItems:'center'},eyebrow:{fontSize:10,fontWeight:'900',letterSpacing:1.8,color:'#777'},title:{fontSize:30,fontWeight:'900',letterSpacing:-1,marginTop:7},copy:{fontSize:12,lineHeight:19,color:'#777',marginTop:5},status:{backgroundColor:'#151515',borderRadius:20,padding:20,marginTop:20},statusLabel:{fontSize:9,fontWeight:'900',letterSpacing:1.2,color:'#aaa'},statusValue:{fontSize:20,fontWeight:'900',color:'#fff',marginTop:6},grid:{flexDirection:'row',flexWrap:'wrap',gap:10,marginTop:16},card:{width:'48%',minHeight:92,backgroundColor:'#fff',borderRadius:17,borderWidth:1,borderColor:'#e5e2dc',padding:15,justifyContent:'space-between'},number:{fontSize:27,fontWeight:'900'},cardLabel:{fontSize:9,fontWeight:'900',letterSpacing:.8,color:'#777'},section:{fontSize:19,fontWeight:'800',marginTop:27,marginBottom:10},action:{height:56,backgroundColor:'#fff',borderRadius:15,borderWidth:1,borderColor:'#e5e2dc',paddingHorizontal:16,flexDirection:'row',alignItems:'center',justifyContent:'space-between',marginBottom:9},actionText:{fontSize:13,fontWeight:'800'},button:{height:52,backgroundColor:'#111',borderRadius:14,paddingHorizontal:20,alignItems:'center',justifyContent:'center',marginTop:18},buttonText:{color:'#fff',fontSize:10,fontWeight:'900'},error:{fontSize:12,color:'#b42318',marginTop:14}});