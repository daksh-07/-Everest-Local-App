import { useEffect, useState } from 'react';
import { ActivityIndicator, ScrollView, StyleSheet, Text, View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { conversations } from '@/lib/messaging';
export default function Messages(){const [items,setItems]=useState<any[]>([]);const [loading,setLoading]=useState(true);const [error,setError]=useState('');useEffect(()=>{(async()=>{try{setItems(await conversations())}catch(e){setError(e instanceof Error?e.message:'Unable to load messages.')}finally{setLoading(false)}})()},[]);return <SafeAreaView style={s.safe}><ScrollView contentContainerStyle={s.page}><Text style={s.eyebrow}>EVEREST LOCAL</Text><Text style={s.title}>Messages</Text><Text style={s.copy}>Conversations are only visible to their authorized participants.</Text>{loading?<ActivityIndicator/>:error?<Text style={s.error}>{error}</Text>:items.length?items.map(x=><View style={s.card} key={x.id}><Text style={s.cardTitle}>Marketplace conversation</Text><Text style={s.meta}>{new Date(x.created_at).toLocaleString()}</Text></View>):<View style={s.empty}><Text style={s.emptyTitle}>No messages yet.</Text><Text style={s.meta}>Messages will appear here after a real marketplace interaction.</Text></View>}</ScrollView></SafeAreaView>}
const s=StyleSheet.create({safe:{flex:1,backgroundColor:'#f8f7f4'},page:{padding:20},eyebrow:{fontSize:10,fontWeight:'900',letterSpacing:2,color:'#777'},title:{fontSize:31,fontWeight:'900',marginTop:7},copy:{fontSize:13,lineHeight:20,color:'#777',marginTop:8,marginBottom:24},card:{backgroundColor:'#fff',borderRadius:17,borderWidth:1,borderColor:'#e5e2dc',padding:17,marginBottom:10},cardTitle:{fontSize:14,fontWeight:'800'},meta:{fontSize:11,color:'#777',marginTop:6},empty:{backgroundColor:'#fff',borderRadius:19,padding:26,alignItems:'center'},emptyTitle:{fontSize:16,fontWeight:'800'},error:{color:'#b42318'}});